extends layout

block content
  .w3-card-4
    header.w3-container.w3-teal
      h1 Periodo #{periodo.nome}
    br
    .w3-container
      table.w3-table-all
        tr
          th Id
          th Nome
          th Data de Nascimento
          th Data de Óbito
        each c in compositores
          tr
            td= c.id
            td
              a(href='/compositores/' + c.id)= c.nome
            td= c.dataNasc
            td= c.dataObito
    br

    footer.w3-container.w3-teal
      address Gerado por EngWeb2024 em #{data} - 
        a(href='/periodos') [Voltar]
